# The shadow DOM

The shadow DOM has its own HTML structure, styles, and JavaScript
that is isolated from the rest of the page.

The shadow DOM can be disabled if needed. For example if you
want to use existing styles.

**Drawback** when the shadow DOM is disabled: No slots, no scoped styles.