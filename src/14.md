# Find existing WebComponents

Search for `@customElement('` in the `Build/Sources/TypeScript`
of the TYPO3 repository.

TYPO3 uses TypeScript. In TypeScript the decorator `@customElement()'`
is equivalent to `customElements.define()` in JavaScript 

**Note:** Not all available components are designed to be re-used. 